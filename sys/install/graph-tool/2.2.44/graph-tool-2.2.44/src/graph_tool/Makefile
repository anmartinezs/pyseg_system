# Makefile.in generated by automake 1.15 from Makefile.am.
# src/graph_tool/Makefile.  Generated from Makefile.in by configure.

# Copyright (C) 1994-2014 Free Software Foundation, Inc.

# This Makefile.in is free software; the Free Software Foundation
# gives unlimited permission to copy and/or distribute it,
# with or without modifications, as long as this notice is preserved.

# This program is distributed in the hope that it will be useful,
# but WITHOUT ANY WARRANTY, to the extent permitted by law; without
# even the implied warranty of MERCHANTABILITY or FITNESS FOR A
# PARTICULAR PURPOSE.




am__is_gnu_make = { \
  if test -z '$(MAKELEVEL)'; then \
    false; \
  elif test -n '$(MAKE_HOST)'; then \
    true; \
  elif test -n '$(MAKE_VERSION)' && test -n '$(CURDIR)'; then \
    true; \
  else \
    false; \
  fi; \
}
am__make_running_with_option = \
  case $${target_option-} in \
      ?) ;; \
      *) echo "am__make_running_with_option: internal error: invalid" \
              "target option '$${target_option-}' specified" >&2; \
         exit 1;; \
  esac; \
  has_opt=no; \
  sane_makeflags=$$MAKEFLAGS; \
  if $(am__is_gnu_make); then \
    sane_makeflags=$$MFLAGS; \
  else \
    case $$MAKEFLAGS in \
      *\\[\ \	]*) \
        bs=\\; \
        sane_makeflags=`printf '%s\n' "$$MAKEFLAGS" \
          | sed "s/$$bs$$bs[$$bs $$bs	]*//g"`;; \
    esac; \
  fi; \
  skip_next=no; \
  strip_trailopt () \
  { \
    flg=`printf '%s\n' "$$flg" | sed "s/$$1.*$$//"`; \
  }; \
  for flg in $$sane_makeflags; do \
    test $$skip_next = yes && { skip_next=no; continue; }; \
    case $$flg in \
      *=*|--*) continue;; \
        -*I) strip_trailopt 'I'; skip_next=yes;; \
      -*I?*) strip_trailopt 'I';; \
        -*O) strip_trailopt 'O'; skip_next=yes;; \
      -*O?*) strip_trailopt 'O';; \
        -*l) strip_trailopt 'l'; skip_next=yes;; \
      -*l?*) strip_trailopt 'l';; \
      -[dEDm]) skip_next=yes;; \
      -[JT]) skip_next=yes;; \
    esac; \
    case $$flg in \
      *$$target_option*) has_opt=yes; break;; \
    esac; \
  done; \
  test $$has_opt = yes
am__make_dryrun = (target_option=n; $(am__make_running_with_option))
am__make_keepgoing = (target_option=k; $(am__make_running_with_option))
pkgdatadir = $(datadir)/graph-tool
pkgincludedir = $(includedir)/graph-tool
pkglibdir = $(libdir)/graph-tool
pkglibexecdir = $(libexecdir)/graph-tool
am__cd = CDPATH="$${ZSH_VERSION+.}$(PATH_SEPARATOR)" && cd
install_sh_DATA = $(install_sh) -c -m 644
install_sh_PROGRAM = $(install_sh) -c
install_sh_SCRIPT = $(install_sh) -c
INSTALL_HEADER = $(INSTALL_DATA)
transform = $(program_transform_name)
NORMAL_INSTALL = :
PRE_INSTALL = :
POST_INSTALL = :
NORMAL_UNINSTALL = :
PRE_UNINSTALL = :
POST_UNINSTALL = :
build_triplet = x86_64-unknown-linux-gnu
host_triplet = x86_64-unknown-linux-gnu
target_triplet = x86_64-unknown-linux-gnu
subdir = src/graph_tool
ACLOCAL_M4 = $(top_srcdir)/aclocal.m4
am__aclocal_m4_deps = $(top_srcdir)/m4/ax_boost_base.m4 \
	$(top_srcdir)/m4/ax_boost_graph.m4 \
	$(top_srcdir)/m4/ax_boost_iostreams.m4 \
	$(top_srcdir)/m4/ax_boost_python.m4 \
	$(top_srcdir)/m4/ax_boost_regex.m4 \
	$(top_srcdir)/m4/ax_python_devel.m4 \
	$(top_srcdir)/m4/ax_python_module.m4 \
	$(top_srcdir)/configure.ac
am__configure_deps = $(am__aclocal_m4_deps) $(CONFIGURE_DEPENDENCIES) \
	$(ACLOCAL_M4)
DIST_COMMON = $(srcdir)/Makefile.am $(graph_tool_PYTHON) \
	$(graph_tool_centrality_PYTHON) \
	$(graph_tool_clustering_PYTHON) \
	$(graph_tool_collection_PYTHON) $(graph_tool_community_PYTHON) \
	$(graph_tool_correlations_PYTHON) $(graph_tool_draw_PYTHON) \
	$(graph_tool_flow_PYTHON) $(graph_tool_generation_PYTHON) \
	$(graph_tool_run_action_PYTHON) $(graph_tool_search_PYTHON) \
	$(graph_tool_spectral_PYTHON) $(graph_tool_stats_PYTHON) \
	$(graph_tool_topology_PYTHON) $(graph_tool_util_PYTHON) \
	$(am__DIST_COMMON)
mkinstalldirs = $(install_sh) -d
CONFIG_HEADER = $(top_builddir)/config.h
CONFIG_CLEAN_FILES =
CONFIG_CLEAN_VPATH_FILES =
AM_V_P = $(am__v_P_$(V))
am__v_P_ = $(am__v_P_$(AM_DEFAULT_VERBOSITY))
am__v_P_0 = false
am__v_P_1 = :
AM_V_GEN = $(am__v_GEN_$(V))
am__v_GEN_ = $(am__v_GEN_$(AM_DEFAULT_VERBOSITY))
am__v_GEN_0 = @echo "  GEN     " $@;
am__v_GEN_1 = 
AM_V_at = $(am__v_at_$(V))
am__v_at_ = $(am__v_at_$(AM_DEFAULT_VERBOSITY))
am__v_at_0 = @
am__v_at_1 = 
SOURCES =
DIST_SOURCES =
am__can_run_installinfo = \
  case $$AM_UPDATE_INFO_DIR in \
    n|no|NO) false;; \
    *) (install-info --version) >/dev/null 2>&1;; \
  esac
am__vpath_adj_setup = srcdirstrip=`echo "$(srcdir)" | sed 's|.|.|g'`;
am__vpath_adj = case $$p in \
    $(srcdir)/*) f=`echo "$$p" | sed "s|^$$srcdirstrip/||"`;; \
    *) f=$$p;; \
  esac;
am__strip_dir = f=`echo $$p | sed -e 's|^.*/||'`;
am__install_max = 40
am__nobase_strip_setup = \
  srcdirstrip=`echo "$(srcdir)" | sed 's/[].[^$$\\*|]/\\\\&/g'`
am__nobase_strip = \
  for p in $$list; do echo "$$p"; done | sed -e "s|$$srcdirstrip/||"
am__nobase_list = $(am__nobase_strip_setup); \
  for p in $$list; do echo "$$p $$p"; done | \
  sed "s| $$srcdirstrip/| |;"' / .*\//!s/ .*/ ./; s,\( .*\)/[^/]*$$,\1,' | \
  $(AWK) 'BEGIN { files["."] = "" } { files[$$2] = files[$$2] " " $$1; \
    if (++n[$$2] == $(am__install_max)) \
      { print $$2, files[$$2]; n[$$2] = 0; files[$$2] = "" } } \
    END { for (dir in files) print dir, files[dir] }'
am__base_list = \
  sed '$$!N;$$!N;$$!N;$$!N;$$!N;$$!N;$$!N;s/\n/ /g' | \
  sed '$$!N;$$!N;$$!N;$$!N;s/\n/ /g'
am__uninstall_files_from_dir = { \
  test -z "$$files" \
    || { test ! -d "$$dir" && test ! -f "$$dir" && test ! -r "$$dir"; } \
    || { echo " ( cd '$$dir' && rm -f" $$files ")"; \
         $(am__cd) "$$dir" && rm -f $$files; }; \
  }
am__py_compile = PYTHON=$(PYTHON) $(SHELL) $(py_compile)
am__installdirs = "$(DESTDIR)$(graph_tooldir)" \
	"$(DESTDIR)$(graph_tool_centralitydir)" \
	"$(DESTDIR)$(graph_tool_clusteringdir)" \
	"$(DESTDIR)$(graph_tool_collectiondir)" \
	"$(DESTDIR)$(graph_tool_communitydir)" \
	"$(DESTDIR)$(graph_tool_correlationsdir)" \
	"$(DESTDIR)$(graph_tool_drawdir)" \
	"$(DESTDIR)$(graph_tool_flowdir)" \
	"$(DESTDIR)$(graph_tool_generationdir)" \
	"$(DESTDIR)$(graph_tool_run_actiondir)" \
	"$(DESTDIR)$(graph_tool_searchdir)" \
	"$(DESTDIR)$(graph_tool_spectraldir)" \
	"$(DESTDIR)$(graph_tool_statsdir)" \
	"$(DESTDIR)$(graph_tool_topologydir)" \
	"$(DESTDIR)$(graph_tool_utildir)" \
	"$(DESTDIR)$(graph_tool_collectiondir)" \
	"$(DESTDIR)$(graph_tool_drawdir)"
am__pep3147_tweak = \
  sed -e 's|\.py$$||' -e 's|[^/]*$$|__pycache__/&.*.py|'
py_compile = $(top_srcdir)/py-compile
DATA = $(graph_tool_collection_DATA) $(graph_tool_draw_DATA)
am__tagged_files = $(HEADERS) $(SOURCES) $(TAGS_FILES) $(LISP)
am__DIST_COMMON = $(srcdir)/Makefile.in $(top_srcdir)/py-compile
DISTFILES = $(DIST_COMMON) $(DIST_SOURCES) $(TEXINFOS) $(EXTRA_DIST)
ACLOCAL = ${SHELL} /home/martinez/workspace/pyseg_git/pyseg_system/sys/install/graph-tool/2.2.44/graph-tool-2.2.44/missing aclocal-1.15
AMTAR = $${TAR-tar}
AM_DEFAULT_VERBOSITY = 0
AR = ar
AUTOCONF = ${SHELL} /home/martinez/workspace/pyseg_git/pyseg_system/sys/install/graph-tool/2.2.44/graph-tool-2.2.44/missing autoconf
AUTOHEADER = ${SHELL} /home/martinez/workspace/pyseg_git/pyseg_system/sys/install/graph-tool/2.2.44/graph-tool-2.2.44/missing autoheader
AUTOMAKE = ${SHELL} /home/martinez/workspace/pyseg_git/pyseg_system/sys/install/graph-tool/2.2.44/graph-tool-2.2.44/missing automake-1.15
AWK = mawk
BOOST_CPPFLAGS = -I/usr/include
BOOST_GRAPH_LIB = boost_graph
BOOST_IOSTREAMS_LIB = boost_iostreams
BOOST_LDFLAGS = -L/usr/lib
BOOST_PYTHON_LIB = boost_python
BOOST_REGEX_LIB = boost_regex
CAIROMM_CFLAGS = -I/usr/include/cairomm-1.0 -I/usr/lib/x86_64-linux-gnu/cairomm-1.0/include -I/usr/include/cairo -I/usr/include/glib-2.0 -I/usr/lib/x86_64-linux-gnu/glib-2.0/include -I/usr/include/pixman-1 -I/usr/include/freetype2 -I/usr/include/libpng16 -I/usr/include/freetype2 -I/usr/include/libpng12 -I/usr/include/sigc++-2.0 -I/usr/lib/x86_64-linux-gnu/sigc++-2.0/include
CAIROMM_LIBS = -lcairomm-1.0 -lcairo -lsigc-2.0
CC = /usr/bin/gcc-4.8
CCDEPMODE = depmode=gcc3
CFLAGS = -g -O2
CGAL_FLAGS = -frounding-math
CGAL_LIBADD = -lCGAL -lgmp
CPP = /usr/bin/gcc-4.8 -E
CPPFLAGS =  -I/home/martinez/anaconda2/include/python2.7 -I/usr/include -I/home/martinez/anaconda2/lib/python2.7/site-packages/numpy/core/include
CXX = /usr/bin/g++-4.8
CXXCPP = /usr/bin/g++-4.8 -E
CXXDEPMODE = depmode=gcc3
CXXFLAGS = -Wall -Wextra -ftemplate-backtrace-limit=0  -DNDEBUG -std=gnu++11 -ftemplate-depth-250 -Wno-deprecated -Wno-unknown-pragmas -O3 -fvisibility=default -fvisibility-inlines-hidden -Wno-unknown-pragmas
CYGPATH_W = echo
DEFS = -DHAVE_CONFIG_H
DEPDIR = .deps
DLLTOOL = false
DSYMUTIL = 
DUMPBIN = 
ECHO_C = 
ECHO_N = -n
ECHO_T = 
EGREP = /bin/grep -E
EXEEXT = 
FGREP = /bin/grep -F
GRAPH_TOOL_VERSION_MAJOR = 2
GRAPH_TOOL_VERSION_MICRO = 44
GRAPH_TOOL_VERSION_MINOR = 2
GREP = /bin/grep
INSTALL = /usr/bin/install -c
INSTALL_DATA = ${INSTALL} -m 644
INSTALL_PROGRAM = ${INSTALL}
INSTALL_SCRIPT = ${INSTALL}
INSTALL_STRIP_PROGRAM = $(install_sh) -c -s
LD = /usr/bin/ld -m elf_x86_64
LDFLAGS =  -L/home/martinez/anaconda2/lib -lpython2.7
LIBOBJS = 
LIBS = -lCGAL -lexpat -lm 
LIBTOOL = $(SHELL) $(top_builddir)/libtool
LIPO = 
LN_S = ln -s
LTLIBOBJS = 
LT_SYS_LIBRARY_PATH = 
MAKEINFO = ${SHELL} /home/martinez/workspace/pyseg_git/pyseg_system/sys/install/graph-tool/2.2.44/graph-tool-2.2.44/missing makeinfo
MANIFEST_TOOL = :
MKDIR_P = /bin/mkdir -p
MOD_CPPFLAGS = -I$(srcdir) -I$(srcdir)/.. -I$(srcdir)/../../boost-workaround -DHAVE_CONFIG_H
MOD_DIR = /home/martinez/anaconda2/lib/python2.7/site-packages/graph_tool
MOD_LDFLAGS = -module -avoid-version -export-dynamic -no-undefined -Wl,-E -Wl,--as-needed
MOD_LIBADD = -L/home/martinez/anaconda2/lib -lpython2.7 -lboost_iostreams -lboost_python -lboost_regex  -lexpat
NM = /usr/bin/nm -B
NMEDIT = 
NO_PREFIX_PACKAGE_DATA_DIR = share/graph-tool
NO_PREFIX_PACKAGE_DOC_DIR = doc/graph-tool
OBJDUMP = objdump
OBJEXT = o
OPENMP_LDFLAGS = 
OTOOL = 
OTOOL64 = 
PACKAGE = graph-tool
PACKAGE_BUGREPORT = http://graph-tool.skewed.de/tickets
PACKAGE_DATA_DIR = /home/martinez/workspace/pyseg_git/pyseg_system/sys/soft/graph-tool/share/graph-tool
PACKAGE_DOC_DIR = ${datarootdir}/doc/${PACKAGE_TARNAME}
PACKAGE_NAME = graph-tool
PACKAGE_STRING = graph-tool 2.2.44
PACKAGE_TARNAME = graph-tool
PACKAGE_URL = http://graph-tool.skewed.de
PACKAGE_VERSION = 2.2.44
PATH_SEPARATOR = :
PKG_CONFIG = /usr/bin/pkg-config
PKG_CONFIG_LIBDIR = 
PKG_CONFIG_PATH = 
PYTHON = /home/martinez/anaconda2/bin/python
PYTHON_CPPFLAGS = -I/home/martinez/anaconda2/include/python2.7
PYTHON_EXEC_PREFIX = ${exec_prefix}
PYTHON_EXTRA_LDFLAGS = -Xlinker -export-dynamic
PYTHON_EXTRA_LIBS = -lpthread -ldl -lutil -lm
PYTHON_FULL_VERSION = 2.7.15
PYTHON_LDFLAGS = -L/home/martinez/anaconda2/lib -lpython2.7
PYTHON_PLATFORM = linux2
PYTHON_PREFIX = ${prefix}
PYTHON_SITE_PKG = /home/martinez/anaconda2/lib/python2.7/site-packages
PYTHON_VERSION = 2.7
RANLIB = ranlib
SED = /bin/sed
SET_MAKE = 
SHELL = /bin/bash
SPARSEHASH_CFLAGS = 
SPARSEHASH_LIBS = 
STRIP = strip
VERSION = 2.2.44
abs_builddir = /home/martinez/workspace/pyseg_git/pyseg_system/sys/install/graph-tool/2.2.44/graph-tool-2.2.44/src/graph_tool
abs_srcdir = /home/martinez/workspace/pyseg_git/pyseg_system/sys/install/graph-tool/2.2.44/graph-tool-2.2.44/src/graph_tool
abs_top_builddir = /home/martinez/workspace/pyseg_git/pyseg_system/sys/install/graph-tool/2.2.44/graph-tool-2.2.44
abs_top_srcdir = /home/martinez/workspace/pyseg_git/pyseg_system/sys/install/graph-tool/2.2.44/graph-tool-2.2.44
ac_ct_AR = ar
ac_ct_CC = /usr/bin/gcc-4.8
ac_ct_CXX = 
ac_ct_DUMPBIN = 
am__include = include
am__leading_dot = .
am__quote = 
am__tar = $${TAR-tar} chof - "$$tardir"
am__untar = $${TAR-tar} xf -
bindir = ${exec_prefix}/bin
build = x86_64-unknown-linux-gnu
build_alias = 
build_cpu = x86_64
build_os = linux-gnu
build_vendor = unknown
builddir = .
datadir = ${datarootdir}
datarootdir = ${prefix}/share
docdir = ${datarootdir}/doc/${PACKAGE_TARNAME}
dvidir = ${docdir}
exec_prefix = ${prefix}
host = x86_64-unknown-linux-gnu
host_alias = 
host_cpu = x86_64
host_os = linux-gnu
host_vendor = unknown
htmldir = ${docdir}
includedir = ${prefix}/include
infodir = ${datarootdir}/info
install_sh = ${SHELL} /home/martinez/workspace/pyseg_git/pyseg_system/sys/install/graph-tool/2.2.44/graph-tool-2.2.44/install-sh
libdir = ${exec_prefix}/lib
libexecdir = ${exec_prefix}/libexec
localedir = ${datarootdir}/locale
localstatedir = ${prefix}/var
mandir = ${datarootdir}/man
mkdir_p = $(MKDIR_P)
oldincludedir = /usr/include
pdfdir = ${docdir}
pkgpyexecdir = ${pyexecdir}/graph-tool
pkgpythondir = ${pythondir}/graph-tool
prefix = /home/martinez/workspace/pyseg_git/pyseg_system/sys/soft/graph-tool
program_transform_name = s,x,x,
psdir = ${docdir}
pyexecdir = ${exec_prefix}/lib/python2.7/site-packages
pythondir = ${prefix}/lib/python2.7/site-packages
sbindir = ${exec_prefix}/sbin
sharedstatedir = ${prefix}/com
srcdir = .
sysconfdir = ${prefix}/etc
target = x86_64-unknown-linux-gnu
target_alias = 
target_cpu = x86_64
target_os = linux-gnu
target_vendor = unknown
top_build_prefix = ../../
top_builddir = ../..
top_srcdir = ../..
graph_tool_PYTHON = \
    __init__.py \
    dl_import.py \
    decorators.py \
    io.py \
    all.py

graph_tooldir = $(MOD_DIR)
graph_tool_run_action_PYTHON = \
    run_action/__init__.py \
    run_action/inline.py \
    run_action/run_action_support.hh \
    run_action/run_action_template.hh

graph_tool_run_actiondir = $(MOD_DIR)/run_action
graph_tool_generation_PYTHON = \
    generation/__init__.py

graph_tool_generationdir = $(MOD_DIR)/generation
graph_tool_collection_PYTHON = \
    collection/__init__.py

graph_tool_collection_DATA = \
    collection/adjnoun.gt.gz \
    collection/as-22july06.gt.gz \
    collection/astro-ph.gt.gz \
    collection/celegansneural.gt.gz \
    collection/cond-mat-2003.gt.gz \
    collection/cond-mat-2005.gt.gz \
    collection/cond-mat.gt.gz \
    collection/dolphins.gt.gz \
    collection/football.gt.gz \
    collection/hep-th.gt.gz \
    collection/karate.gt.gz \
    collection/lesmis.gt.gz \
    collection/netscience.gt.gz \
    collection/polblogs.gt.gz \
    collection/polbooks.gt.gz \
    collection/power.gt.gz \
    collection/pgp-strong-2009.gt.gz \
    collection/serengeti-foodweb.gt.gz \
    collection/email-Enron.gt.gz

graph_tool_collectiondir = $(MOD_DIR)/collection
graph_tool_correlations_PYTHON = \
    correlations/__init__.py

graph_tool_correlationsdir = $(MOD_DIR)/correlations
graph_tool_stats_PYTHON = \
    stats/__init__.py

graph_tool_statsdir = $(MOD_DIR)/stats
graph_tool_clustering_PYTHON = \
    clustering/__init__.py

graph_tool_clusteringdir = $(MOD_DIR)/clustering
graph_tool_centrality_PYTHON = \
    centrality/__init__.py

graph_tool_centralitydir = $(MOD_DIR)/centrality
graph_tool_community_PYTHON = \
    community/__init__.py \
    community/blockmodel.py \
    community/covariate_blockmodel.py \
    community/nested_blockmodel.py \
    community/overlap_blockmodel.py

graph_tool_communitydir = $(MOD_DIR)/community
graph_tool_draw_PYTHON = \
    draw/__init__.py \
    draw/cairo_draw.py \
    draw/gtk_draw.py \
    draw/graphviz_draw.py

graph_tool_draw_DATA = \
    draw/graph-tool-logo.svg

graph_tool_drawdir = $(MOD_DIR)/draw
graph_tool_util_PYTHON = \
    util/__init__.py

graph_tool_utildir = $(MOD_DIR)/util
graph_tool_topology_PYTHON = \
    topology/__init__.py

graph_tool_topologydir = $(MOD_DIR)/topology
graph_tool_flow_PYTHON = \
    flow/__init__.py

graph_tool_flowdir = $(MOD_DIR)/flow
graph_tool_spectral_PYTHON = \
    spectral/__init__.py

graph_tool_spectraldir = $(MOD_DIR)/spectral
graph_tool_search_PYTHON = \
    search/__init__.py

graph_tool_searchdir = $(MOD_DIR)/search
EXTRA_DIST = $(graph_tool_draw_DATA) $(graph_tool_collection_DATA)
all: all-am

.SUFFIXES:
$(srcdir)/Makefile.in:  $(srcdir)/Makefile.am  $(am__configure_deps)
	@for dep in $?; do \
	  case '$(am__configure_deps)' in \
	    *$$dep*) \
	      ( cd $(top_builddir) && $(MAKE) $(AM_MAKEFLAGS) am--refresh ) \
	        && { if test -f $@; then exit 0; else break; fi; }; \
	      exit 1;; \
	  esac; \
	done; \
	echo ' cd $(top_srcdir) && $(AUTOMAKE) --foreign src/graph_tool/Makefile'; \
	$(am__cd) $(top_srcdir) && \
	  $(AUTOMAKE) --foreign src/graph_tool/Makefile
Makefile: $(srcdir)/Makefile.in $(top_builddir)/config.status
	@case '$?' in \
	  *config.status*) \
	    cd $(top_builddir) && $(MAKE) $(AM_MAKEFLAGS) am--refresh;; \
	  *) \
	    echo ' cd $(top_builddir) && $(SHELL) ./config.status $(subdir)/$@ $(am__depfiles_maybe)'; \
	    cd $(top_builddir) && $(SHELL) ./config.status $(subdir)/$@ $(am__depfiles_maybe);; \
	esac;

$(top_builddir)/config.status: $(top_srcdir)/configure $(CONFIG_STATUS_DEPENDENCIES)
	cd $(top_builddir) && $(MAKE) $(AM_MAKEFLAGS) am--refresh

$(top_srcdir)/configure:  $(am__configure_deps)
	cd $(top_builddir) && $(MAKE) $(AM_MAKEFLAGS) am--refresh
$(ACLOCAL_M4):  $(am__aclocal_m4_deps)
	cd $(top_builddir) && $(MAKE) $(AM_MAKEFLAGS) am--refresh
$(am__aclocal_m4_deps):

mostlyclean-libtool:
	-rm -f *.lo

clean-libtool:
	-rm -rf .libs _libs
install-graph_toolPYTHON: $(graph_tool_PYTHON)
	@$(NORMAL_INSTALL)
	@list='$(graph_tool_PYTHON)'; dlist=; list2=; test -n "$(graph_tooldir)" || list=; \
	if test -n "$$list"; then \
	  echo " $(MKDIR_P) '$(DESTDIR)$(graph_tooldir)'"; \
	  $(MKDIR_P) "$(DESTDIR)$(graph_tooldir)" || exit 1; \
	fi; \
	for p in $$list; do \
	  if test -f "$$p"; then b=; else b="$(srcdir)/"; fi; \
	  if test -f $$b$$p; then \
	    $(am__strip_dir) \
	    dlist="$$dlist $$f"; \
	    list2="$$list2 $$b$$p"; \
	  else :; fi; \
	done; \
	for file in $$list2; do echo $$file; done | $(am__base_list) | \
	while read files; do \
	  echo " $(INSTALL_DATA) $$files '$(DESTDIR)$(graph_tooldir)'"; \
	  $(INSTALL_DATA) $$files "$(DESTDIR)$(graph_tooldir)" || exit $$?; \
	done || exit $$?; \
	if test -n "$$dlist"; then \
	  $(am__py_compile) --destdir "$(DESTDIR)" \
	                    --basedir "$(graph_tooldir)" $$dlist; \
	else :; fi

uninstall-graph_toolPYTHON:
	@$(NORMAL_UNINSTALL)
	@list='$(graph_tool_PYTHON)'; test -n "$(graph_tooldir)" || list=; \
	py_files=`for p in $$list; do echo $$p; done | sed -e 's|^.*/||'`; \
	test -n "$$py_files" || exit 0; \
	dir='$(DESTDIR)$(graph_tooldir)'; \
	pyc_files=`echo "$$py_files" | sed 's|$$|c|'`; \
	pyo_files=`echo "$$py_files" | sed 's|$$|o|'`; \
	py_files_pep3147=`echo "$$py_files" | $(am__pep3147_tweak)`; \
	echo "$$py_files_pep3147";\
	pyc_files_pep3147=`echo "$$py_files_pep3147" | sed 's|$$|c|'`; \
	pyo_files_pep3147=`echo "$$py_files_pep3147" | sed 's|$$|o|'`; \
	st=0; \
	for files in \
	  "$$py_files" \
	  "$$pyc_files" \
	  "$$pyo_files" \
	  "$$pyc_files_pep3147" \
	  "$$pyo_files_pep3147" \
	; do \
	  $(am__uninstall_files_from_dir) || st=$$?; \
	done; \
	exit $$st
install-graph_tool_centralityPYTHON: $(graph_tool_centrality_PYTHON)
	@$(NORMAL_INSTALL)
	@list='$(graph_tool_centrality_PYTHON)'; dlist=; list2=; test -n "$(graph_tool_centralitydir)" || list=; \
	if test -n "$$list"; then \
	  echo " $(MKDIR_P) '$(DESTDIR)$(graph_tool_centralitydir)'"; \
	  $(MKDIR_P) "$(DESTDIR)$(graph_tool_centralitydir)" || exit 1; \
	fi; \
	for p in $$list; do \
	  if test -f "$$p"; then b=; else b="$(srcdir)/"; fi; \
	  if test -f $$b$$p; then \
	    $(am__strip_dir) \
	    dlist="$$dlist $$f"; \
	    list2="$$list2 $$b$$p"; \
	  else :; fi; \
	done; \
	for file in $$list2; do echo $$file; done | $(am__base_list) | \
	while read files; do \
	  echo " $(INSTALL_DATA) $$files '$(DESTDIR)$(graph_tool_centralitydir)'"; \
	  $(INSTALL_DATA) $$files "$(DESTDIR)$(graph_tool_centralitydir)" || exit $$?; \
	done || exit $$?; \
	if test -n "$$dlist"; then \
	  $(am__py_compile) --destdir "$(DESTDIR)" \
	                    --basedir "$(graph_tool_centralitydir)" $$dlist; \
	else :; fi

uninstall-graph_tool_centralityPYTHON:
	@$(NORMAL_UNINSTALL)
	@list='$(graph_tool_centrality_PYTHON)'; test -n "$(graph_tool_centralitydir)" || list=; \
	py_files=`for p in $$list; do echo $$p; done | sed -e 's|^.*/||'`; \
	test -n "$$py_files" || exit 0; \
	dir='$(DESTDIR)$(graph_tool_centralitydir)'; \
	pyc_files=`echo "$$py_files" | sed 's|$$|c|'`; \
	pyo_files=`echo "$$py_files" | sed 's|$$|o|'`; \
	py_files_pep3147=`echo "$$py_files" | $(am__pep3147_tweak)`; \
	echo "$$py_files_pep3147";\
	pyc_files_pep3147=`echo "$$py_files_pep3147" | sed 's|$$|c|'`; \
	pyo_files_pep3147=`echo "$$py_files_pep3147" | sed 's|$$|o|'`; \
	st=0; \
	for files in \
	  "$$py_files" \
	  "$$pyc_files" \
	  "$$pyo_files" \
	  "$$pyc_files_pep3147" \
	  "$$pyo_files_pep3147" \
	; do \
	  $(am__uninstall_files_from_dir) || st=$$?; \
	done; \
	exit $$st
install-graph_tool_clusteringPYTHON: $(graph_tool_clustering_PYTHON)
	@$(NORMAL_INSTALL)
	@list='$(graph_tool_clustering_PYTHON)'; dlist=; list2=; test -n "$(graph_tool_clusteringdir)" || list=; \
	if test -n "$$list"; then \
	  echo " $(MKDIR_P) '$(DESTDIR)$(graph_tool_clusteringdir)'"; \
	  $(MKDIR_P) "$(DESTDIR)$(graph_tool_clusteringdir)" || exit 1; \
	fi; \
	for p in $$list; do \
	  if test -f "$$p"; then b=; else b="$(srcdir)/"; fi; \
	  if test -f $$b$$p; then \
	    $(am__strip_dir) \
	    dlist="$$dlist $$f"; \
	    list2="$$list2 $$b$$p"; \
	  else :; fi; \
	done; \
	for file in $$list2; do echo $$file; done | $(am__base_list) | \
	while read files; do \
	  echo " $(INSTALL_DATA) $$files '$(DESTDIR)$(graph_tool_clusteringdir)'"; \
	  $(INSTALL_DATA) $$files "$(DESTDIR)$(graph_tool_clusteringdir)" || exit $$?; \
	done || exit $$?; \
	if test -n "$$dlist"; then \
	  $(am__py_compile) --destdir "$(DESTDIR)" \
	                    --basedir "$(graph_tool_clusteringdir)" $$dlist; \
	else :; fi

uninstall-graph_tool_clusteringPYTHON:
	@$(NORMAL_UNINSTALL)
	@list='$(graph_tool_clustering_PYTHON)'; test -n "$(graph_tool_clusteringdir)" || list=; \
	py_files=`for p in $$list; do echo $$p; done | sed -e 's|^.*/||'`; \
	test -n "$$py_files" || exit 0; \
	dir='$(DESTDIR)$(graph_tool_clusteringdir)'; \
	pyc_files=`echo "$$py_files" | sed 's|$$|c|'`; \
	pyo_files=`echo "$$py_files" | sed 's|$$|o|'`; \
	py_files_pep3147=`echo "$$py_files" | $(am__pep3147_tweak)`; \
	echo "$$py_files_pep3147";\
	pyc_files_pep3147=`echo "$$py_files_pep3147" | sed 's|$$|c|'`; \
	pyo_files_pep3147=`echo "$$py_files_pep3147" | sed 's|$$|o|'`; \
	st=0; \
	for files in \
	  "$$py_files" \
	  "$$pyc_files" \
	  "$$pyo_files" \
	  "$$pyc_files_pep3147" \
	  "$$pyo_files_pep3147" \
	; do \
	  $(am__uninstall_files_from_dir) || st=$$?; \
	done; \
	exit $$st
install-graph_tool_collectionPYTHON: $(graph_tool_collection_PYTHON)
	@$(NORMAL_INSTALL)
	@list='$(graph_tool_collection_PYTHON)'; dlist=; list2=; test -n "$(graph_tool_collectiondir)" || list=; \
	if test -n "$$list"; then \
	  echo " $(MKDIR_P) '$(DESTDIR)$(graph_tool_collectiondir)'"; \
	  $(MKDIR_P) "$(DESTDIR)$(graph_tool_collectiondir)" || exit 1; \
	fi; \
	for p in $$list; do \
	  if test -f "$$p"; then b=; else b="$(srcdir)/"; fi; \
	  if test -f $$b$$p; then \
	    $(am__strip_dir) \
	    dlist="$$dlist $$f"; \
	    list2="$$list2 $$b$$p"; \
	  else :; fi; \
	done; \
	for file in $$list2; do echo $$file; done | $(am__base_list) | \
	while read files; do \
	  echo " $(INSTALL_DATA) $$files '$(DESTDIR)$(graph_tool_collectiondir)'"; \
	  $(INSTALL_DATA) $$files "$(DESTDIR)$(graph_tool_collectiondir)" || exit $$?; \
	done || exit $$?; \
	if test -n "$$dlist"; then \
	  $(am__py_compile) --destdir "$(DESTDIR)" \
	                    --basedir "$(graph_tool_collectiondir)" $$dlist; \
	else :; fi

uninstall-graph_tool_collectionPYTHON:
	@$(NORMAL_UNINSTALL)
	@list='$(graph_tool_collection_PYTHON)'; test -n "$(graph_tool_collectiondir)" || list=; \
	py_files=`for p in $$list; do echo $$p; done | sed -e 's|^.*/||'`; \
	test -n "$$py_files" || exit 0; \
	dir='$(DESTDIR)$(graph_tool_collectiondir)'; \
	pyc_files=`echo "$$py_files" | sed 's|$$|c|'`; \
	pyo_files=`echo "$$py_files" | sed 's|$$|o|'`; \
	py_files_pep3147=`echo "$$py_files" | $(am__pep3147_tweak)`; \
	echo "$$py_files_pep3147";\
	pyc_files_pep3147=`echo "$$py_files_pep3147" | sed 's|$$|c|'`; \
	pyo_files_pep3147=`echo "$$py_files_pep3147" | sed 's|$$|o|'`; \
	st=0; \
	for files in \
	  "$$py_files" \
	  "$$pyc_files" \
	  "$$pyo_files" \
	  "$$pyc_files_pep3147" \
	  "$$pyo_files_pep3147" \
	; do \
	  $(am__uninstall_files_from_dir) || st=$$?; \
	done; \
	exit $$st
install-graph_tool_communityPYTHON: $(graph_tool_community_PYTHON)
	@$(NORMAL_INSTALL)
	@list='$(graph_tool_community_PYTHON)'; dlist=; list2=; test -n "$(graph_tool_communitydir)" || list=; \
	if test -n "$$list"; then \
	  echo " $(MKDIR_P) '$(DESTDIR)$(graph_tool_communitydir)'"; \
	  $(MKDIR_P) "$(DESTDIR)$(graph_tool_communitydir)" || exit 1; \
	fi; \
	for p in $$list; do \
	  if test -f "$$p"; then b=; else b="$(srcdir)/"; fi; \
	  if test -f $$b$$p; then \
	    $(am__strip_dir) \
	    dlist="$$dlist $$f"; \
	    list2="$$list2 $$b$$p"; \
	  else :; fi; \
	done; \
	for file in $$list2; do echo $$file; done | $(am__base_list) | \
	while read files; do \
	  echo " $(INSTALL_DATA) $$files '$(DESTDIR)$(graph_tool_communitydir)'"; \
	  $(INSTALL_DATA) $$files "$(DESTDIR)$(graph_tool_communitydir)" || exit $$?; \
	done || exit $$?; \
	if test -n "$$dlist"; then \
	  $(am__py_compile) --destdir "$(DESTDIR)" \
	                    --basedir "$(graph_tool_communitydir)" $$dlist; \
	else :; fi

uninstall-graph_tool_communityPYTHON:
	@$(NORMAL_UNINSTALL)
	@list='$(graph_tool_community_PYTHON)'; test -n "$(graph_tool_communitydir)" || list=; \
	py_files=`for p in $$list; do echo $$p; done | sed -e 's|^.*/||'`; \
	test -n "$$py_files" || exit 0; \
	dir='$(DESTDIR)$(graph_tool_communitydir)'; \
	pyc_files=`echo "$$py_files" | sed 's|$$|c|'`; \
	pyo_files=`echo "$$py_files" | sed 's|$$|o|'`; \
	py_files_pep3147=`echo "$$py_files" | $(am__pep3147_tweak)`; \
	echo "$$py_files_pep3147";\
	pyc_files_pep3147=`echo "$$py_files_pep3147" | sed 's|$$|c|'`; \
	pyo_files_pep3147=`echo "$$py_files_pep3147" | sed 's|$$|o|'`; \
	st=0; \
	for files in \
	  "$$py_files" \
	  "$$pyc_files" \
	  "$$pyo_files" \
	  "$$pyc_files_pep3147" \
	  "$$pyo_files_pep3147" \
	; do \
	  $(am__uninstall_files_from_dir) || st=$$?; \
	done; \
	exit $$st
install-graph_tool_correlationsPYTHON: $(graph_tool_correlations_PYTHON)
	@$(NORMAL_INSTALL)
	@list='$(graph_tool_correlations_PYTHON)'; dlist=; list2=; test -n "$(graph_tool_correlationsdir)" || list=; \
	if test -n "$$list"; then \
	  echo " $(MKDIR_P) '$(DESTDIR)$(graph_tool_correlationsdir)'"; \
	  $(MKDIR_P) "$(DESTDIR)$(graph_tool_correlationsdir)" || exit 1; \
	fi; \
	for p in $$list; do \
	  if test -f "$$p"; then b=; else b="$(srcdir)/"; fi; \
	  if test -f $$b$$p; then \
	    $(am__strip_dir) \
	    dlist="$$dlist $$f"; \
	    list2="$$list2 $$b$$p"; \
	  else :; fi; \
	done; \
	for file in $$list2; do echo $$file; done | $(am__base_list) | \
	while read files; do \
	  echo " $(INSTALL_DATA) $$files '$(DESTDIR)$(graph_tool_correlationsdir)'"; \
	  $(INSTALL_DATA) $$files "$(DESTDIR)$(graph_tool_correlationsdir)" || exit $$?; \
	done || exit $$?; \
	if test -n "$$dlist"; then \
	  $(am__py_compile) --destdir "$(DESTDIR)" \
	                    --basedir "$(graph_tool_correlationsdir)" $$dlist; \
	else :; fi

uninstall-graph_tool_correlationsPYTHON:
	@$(NORMAL_UNINSTALL)
	@list='$(graph_tool_correlations_PYTHON)'; test -n "$(graph_tool_correlationsdir)" || list=; \
	py_files=`for p in $$list; do echo $$p; done | sed -e 's|^.*/||'`; \
	test -n "$$py_files" || exit 0; \
	dir='$(DESTDIR)$(graph_tool_correlationsdir)'; \
	pyc_files=`echo "$$py_files" | sed 's|$$|c|'`; \
	pyo_files=`echo "$$py_files" | sed 's|$$|o|'`; \
	py_files_pep3147=`echo "$$py_files" | $(am__pep3147_tweak)`; \
	echo "$$py_files_pep3147";\
	pyc_files_pep3147=`echo "$$py_files_pep3147" | sed 's|$$|c|'`; \
	pyo_files_pep3147=`echo "$$py_files_pep3147" | sed 's|$$|o|'`; \
	st=0; \
	for files in \
	  "$$py_files" \
	  "$$pyc_files" \
	  "$$pyo_files" \
	  "$$pyc_files_pep3147" \
	  "$$pyo_files_pep3147" \
	; do \
	  $(am__uninstall_files_from_dir) || st=$$?; \
	done; \
	exit $$st
install-graph_tool_drawPYTHON: $(graph_tool_draw_PYTHON)
	@$(NORMAL_INSTALL)
	@list='$(graph_tool_draw_PYTHON)'; dlist=; list2=; test -n "$(graph_tool_drawdir)" || list=; \
	if test -n "$$list"; then \
	  echo " $(MKDIR_P) '$(DESTDIR)$(graph_tool_drawdir)'"; \
	  $(MKDIR_P) "$(DESTDIR)$(graph_tool_drawdir)" || exit 1; \
	fi; \
	for p in $$list; do \
	  if test -f "$$p"; then b=; else b="$(srcdir)/"; fi; \
	  if test -f $$b$$p; then \
	    $(am__strip_dir) \
	    dlist="$$dlist $$f"; \
	    list2="$$list2 $$b$$p"; \
	  else :; fi; \
	done; \
	for file in $$list2; do echo $$file; done | $(am__base_list) | \
	while read files; do \
	  echo " $(INSTALL_DATA) $$files '$(DESTDIR)$(graph_tool_drawdir)'"; \
	  $(INSTALL_DATA) $$files "$(DESTDIR)$(graph_tool_drawdir)" || exit $$?; \
	done || exit $$?; \
	if test -n "$$dlist"; then \
	  $(am__py_compile) --destdir "$(DESTDIR)" \
	                    --basedir "$(graph_tool_drawdir)" $$dlist; \
	else :; fi

uninstall-graph_tool_drawPYTHON:
	@$(NORMAL_UNINSTALL)
	@list='$(graph_tool_draw_PYTHON)'; test -n "$(graph_tool_drawdir)" || list=; \
	py_files=`for p in $$list; do echo $$p; done | sed -e 's|^.*/||'`; \
	test -n "$$py_files" || exit 0; \
	dir='$(DESTDIR)$(graph_tool_drawdir)'; \
	pyc_files=`echo "$$py_files" | sed 's|$$|c|'`; \
	pyo_files=`echo "$$py_files" | sed 's|$$|o|'`; \
	py_files_pep3147=`echo "$$py_files" | $(am__pep3147_tweak)`; \
	echo "$$py_files_pep3147";\
	pyc_files_pep3147=`echo "$$py_files_pep3147" | sed 's|$$|c|'`; \
	pyo_files_pep3147=`echo "$$py_files_pep3147" | sed 's|$$|o|'`; \
	st=0; \
	for files in \
	  "$$py_files" \
	  "$$pyc_files" \
	  "$$pyo_files" \
	  "$$pyc_files_pep3147" \
	  "$$pyo_files_pep3147" \
	; do \
	  $(am__uninstall_files_from_dir) || st=$$?; \
	done; \
	exit $$st
install-graph_tool_flowPYTHON: $(graph_tool_flow_PYTHON)
	@$(NORMAL_INSTALL)
	@list='$(graph_tool_flow_PYTHON)'; dlist=; list2=; test -n "$(graph_tool_flowdir)" || list=; \
	if test -n "$$list"; then \
	  echo " $(MKDIR_P) '$(DESTDIR)$(graph_tool_flowdir)'"; \
	  $(MKDIR_P) "$(DESTDIR)$(graph_tool_flowdir)" || exit 1; \
	fi; \
	for p in $$list; do \
	  if test -f "$$p"; then b=; else b="$(srcdir)/"; fi; \
	  if test -f $$b$$p; then \
	    $(am__strip_dir) \
	    dlist="$$dlist $$f"; \
	    list2="$$list2 $$b$$p"; \
	  else :; fi; \
	done; \
	for file in $$list2; do echo $$file; done | $(am__base_list) | \
	while read files; do \
	  echo " $(INSTALL_DATA) $$files '$(DESTDIR)$(graph_tool_flowdir)'"; \
	  $(INSTALL_DATA) $$files "$(DESTDIR)$(graph_tool_flowdir)" || exit $$?; \
	done || exit $$?; \
	if test -n "$$dlist"; then \
	  $(am__py_compile) --destdir "$(DESTDIR)" \
	                    --basedir "$(graph_tool_flowdir)" $$dlist; \
	else :; fi

uninstall-graph_tool_flowPYTHON:
	@$(NORMAL_UNINSTALL)
	@list='$(graph_tool_flow_PYTHON)'; test -n "$(graph_tool_flowdir)" || list=; \
	py_files=`for p in $$list; do echo $$p; done | sed -e 's|^.*/||'`; \
	test -n "$$py_files" || exit 0; \
	dir='$(DESTDIR)$(graph_tool_flowdir)'; \
	pyc_files=`echo "$$py_files" | sed 's|$$|c|'`; \
	pyo_files=`echo "$$py_files" | sed 's|$$|o|'`; \
	py_files_pep3147=`echo "$$py_files" | $(am__pep3147_tweak)`; \
	echo "$$py_files_pep3147";\
	pyc_files_pep3147=`echo "$$py_files_pep3147" | sed 's|$$|c|'`; \
	pyo_files_pep3147=`echo "$$py_files_pep3147" | sed 's|$$|o|'`; \
	st=0; \
	for files in \
	  "$$py_files" \
	  "$$pyc_files" \
	  "$$pyo_files" \
	  "$$pyc_files_pep3147" \
	  "$$pyo_files_pep3147" \
	; do \
	  $(am__uninstall_files_from_dir) || st=$$?; \
	done; \
	exit $$st
install-graph_tool_generationPYTHON: $(graph_tool_generation_PYTHON)
	@$(NORMAL_INSTALL)
	@list='$(graph_tool_generation_PYTHON)'; dlist=; list2=; test -n "$(graph_tool_generationdir)" || list=; \
	if test -n "$$list"; then \
	  echo " $(MKDIR_P) '$(DESTDIR)$(graph_tool_generationdir)'"; \
	  $(MKDIR_P) "$(DESTDIR)$(graph_tool_generationdir)" || exit 1; \
	fi; \
	for p in $$list; do \
	  if test -f "$$p"; then b=; else b="$(srcdir)/"; fi; \
	  if test -f $$b$$p; then \
	    $(am__strip_dir) \
	    dlist="$$dlist $$f"; \
	    list2="$$list2 $$b$$p"; \
	  else :; fi; \
	done; \
	for file in $$list2; do echo $$file; done | $(am__base_list) | \
	while read files; do \
	  echo " $(INSTALL_DATA) $$files '$(DESTDIR)$(graph_tool_generationdir)'"; \
	  $(INSTALL_DATA) $$files "$(DESTDIR)$(graph_tool_generationdir)" || exit $$?; \
	done || exit $$?; \
	if test -n "$$dlist"; then \
	  $(am__py_compile) --destdir "$(DESTDIR)" \
	                    --basedir "$(graph_tool_generationdir)" $$dlist; \
	else :; fi

uninstall-graph_tool_generationPYTHON:
	@$(NORMAL_UNINSTALL)
	@list='$(graph_tool_generation_PYTHON)'; test -n "$(graph_tool_generationdir)" || list=; \
	py_files=`for p in $$list; do echo $$p; done | sed -e 's|^.*/||'`; \
	test -n "$$py_files" || exit 0; \
	dir='$(DESTDIR)$(graph_tool_generationdir)'; \
	pyc_files=`echo "$$py_files" | sed 's|$$|c|'`; \
	pyo_files=`echo "$$py_files" | sed 's|$$|o|'`; \
	py_files_pep3147=`echo "$$py_files" | $(am__pep3147_tweak)`; \
	echo "$$py_files_pep3147";\
	pyc_files_pep3147=`echo "$$py_files_pep3147" | sed 's|$$|c|'`; \
	pyo_files_pep3147=`echo "$$py_files_pep3147" | sed 's|$$|o|'`; \
	st=0; \
	for files in \
	  "$$py_files" \
	  "$$pyc_files" \
	  "$$pyo_files" \
	  "$$pyc_files_pep3147" \
	  "$$pyo_files_pep3147" \
	; do \
	  $(am__uninstall_files_from_dir) || st=$$?; \
	done; \
	exit $$st
install-graph_tool_run_actionPYTHON: $(graph_tool_run_action_PYTHON)
	@$(NORMAL_INSTALL)
	@list='$(graph_tool_run_action_PYTHON)'; dlist=; list2=; test -n "$(graph_tool_run_actiondir)" || list=; \
	if test -n "$$list"; then \
	  echo " $(MKDIR_P) '$(DESTDIR)$(graph_tool_run_actiondir)'"; \
	  $(MKDIR_P) "$(DESTDIR)$(graph_tool_run_actiondir)" || exit 1; \
	fi; \
	for p in $$list; do \
	  if test -f "$$p"; then b=; else b="$(srcdir)/"; fi; \
	  if test -f $$b$$p; then \
	    $(am__strip_dir) \
	    dlist="$$dlist $$f"; \
	    list2="$$list2 $$b$$p"; \
	  else :; fi; \
	done; \
	for file in $$list2; do echo $$file; done | $(am__base_list) | \
	while read files; do \
	  echo " $(INSTALL_DATA) $$files '$(DESTDIR)$(graph_tool_run_actiondir)'"; \
	  $(INSTALL_DATA) $$files "$(DESTDIR)$(graph_tool_run_actiondir)" || exit $$?; \
	done || exit $$?; \
	if test -n "$$dlist"; then \
	  $(am__py_compile) --destdir "$(DESTDIR)" \
	                    --basedir "$(graph_tool_run_actiondir)" $$dlist; \
	else :; fi

uninstall-graph_tool_run_actionPYTHON:
	@$(NORMAL_UNINSTALL)
	@list='$(graph_tool_run_action_PYTHON)'; test -n "$(graph_tool_run_actiondir)" || list=; \
	py_files=`for p in $$list; do echo $$p; done | sed -e 's|^.*/||'`; \
	test -n "$$py_files" || exit 0; \
	dir='$(DESTDIR)$(graph_tool_run_actiondir)'; \
	pyc_files=`echo "$$py_files" | sed 's|$$|c|'`; \
	pyo_files=`echo "$$py_files" | sed 's|$$|o|'`; \
	py_files_pep3147=`echo "$$py_files" | $(am__pep3147_tweak)`; \
	echo "$$py_files_pep3147";\
	pyc_files_pep3147=`echo "$$py_files_pep3147" | sed 's|$$|c|'`; \
	pyo_files_pep3147=`echo "$$py_files_pep3147" | sed 's|$$|o|'`; \
	st=0; \
	for files in \
	  "$$py_files" \
	  "$$pyc_files" \
	  "$$pyo_files" \
	  "$$pyc_files_pep3147" \
	  "$$pyo_files_pep3147" \
	; do \
	  $(am__uninstall_files_from_dir) || st=$$?; \
	done; \
	exit $$st
install-graph_tool_searchPYTHON: $(graph_tool_search_PYTHON)
	@$(NORMAL_INSTALL)
	@list='$(graph_tool_search_PYTHON)'; dlist=; list2=; test -n "$(graph_tool_searchdir)" || list=; \
	if test -n "$$list"; then \
	  echo " $(MKDIR_P) '$(DESTDIR)$(graph_tool_searchdir)'"; \
	  $(MKDIR_P) "$(DESTDIR)$(graph_tool_searchdir)" || exit 1; \
	fi; \
	for p in $$list; do \
	  if test -f "$$p"; then b=; else b="$(srcdir)/"; fi; \
	  if test -f $$b$$p; then \
	    $(am__strip_dir) \
	    dlist="$$dlist $$f"; \
	    list2="$$list2 $$b$$p"; \
	  else :; fi; \
	done; \
	for file in $$list2; do echo $$file; done | $(am__base_list) | \
	while read files; do \
	  echo " $(INSTALL_DATA) $$files '$(DESTDIR)$(graph_tool_searchdir)'"; \
	  $(INSTALL_DATA) $$files "$(DESTDIR)$(graph_tool_searchdir)" || exit $$?; \
	done || exit $$?; \
	if test -n "$$dlist"; then \
	  $(am__py_compile) --destdir "$(DESTDIR)" \
	                    --basedir "$(graph_tool_searchdir)" $$dlist; \
	else :; fi

uninstall-graph_tool_searchPYTHON:
	@$(NORMAL_UNINSTALL)
	@list='$(graph_tool_search_PYTHON)'; test -n "$(graph_tool_searchdir)" || list=; \
	py_files=`for p in $$list; do echo $$p; done | sed -e 's|^.*/||'`; \
	test -n "$$py_files" || exit 0; \
	dir='$(DESTDIR)$(graph_tool_searchdir)'; \
	pyc_files=`echo "$$py_files" | sed 's|$$|c|'`; \
	pyo_files=`echo "$$py_files" | sed 's|$$|o|'`; \
	py_files_pep3147=`echo "$$py_files" | $(am__pep3147_tweak)`; \
	echo "$$py_files_pep3147";\
	pyc_files_pep3147=`echo "$$py_files_pep3147" | sed 's|$$|c|'`; \
	pyo_files_pep3147=`echo "$$py_files_pep3147" | sed 's|$$|o|'`; \
	st=0; \
	for files in \
	  "$$py_files" \
	  "$$pyc_files" \
	  "$$pyo_files" \
	  "$$pyc_files_pep3147" \
	  "$$pyo_files_pep3147" \
	; do \
	  $(am__uninstall_files_from_dir) || st=$$?; \
	done; \
	exit $$st
install-graph_tool_spectralPYTHON: $(graph_tool_spectral_PYTHON)
	@$(NORMAL_INSTALL)
	@list='$(graph_tool_spectral_PYTHON)'; dlist=; list2=; test -n "$(graph_tool_spectraldir)" || list=; \
	if test -n "$$list"; then \
	  echo " $(MKDIR_P) '$(DESTDIR)$(graph_tool_spectraldir)'"; \
	  $(MKDIR_P) "$(DESTDIR)$(graph_tool_spectraldir)" || exit 1; \
	fi; \
	for p in $$list; do \
	  if test -f "$$p"; then b=; else b="$(srcdir)/"; fi; \
	  if test -f $$b$$p; then \
	    $(am__strip_dir) \
	    dlist="$$dlist $$f"; \
	    list2="$$list2 $$b$$p"; \
	  else :; fi; \
	done; \
	for file in $$list2; do echo $$file; done | $(am__base_list) | \
	while read files; do \
	  echo " $(INSTALL_DATA) $$files '$(DESTDIR)$(graph_tool_spectraldir)'"; \
	  $(INSTALL_DATA) $$files "$(DESTDIR)$(graph_tool_spectraldir)" || exit $$?; \
	done || exit $$?; \
	if test -n "$$dlist"; then \
	  $(am__py_compile) --destdir "$(DESTDIR)" \
	                    --basedir "$(graph_tool_spectraldir)" $$dlist; \
	else :; fi

uninstall-graph_tool_spectralPYTHON:
	@$(NORMAL_UNINSTALL)
	@list='$(graph_tool_spectral_PYTHON)'; test -n "$(graph_tool_spectraldir)" || list=; \
	py_files=`for p in $$list; do echo $$p; done | sed -e 's|^.*/||'`; \
	test -n "$$py_files" || exit 0; \
	dir='$(DESTDIR)$(graph_tool_spectraldir)'; \
	pyc_files=`echo "$$py_files" | sed 's|$$|c|'`; \
	pyo_files=`echo "$$py_files" | sed 's|$$|o|'`; \
	py_files_pep3147=`echo "$$py_files" | $(am__pep3147_tweak)`; \
	echo "$$py_files_pep3147";\
	pyc_files_pep3147=`echo "$$py_files_pep3147" | sed 's|$$|c|'`; \
	pyo_files_pep3147=`echo "$$py_files_pep3147" | sed 's|$$|o|'`; \
	st=0; \
	for files in \
	  "$$py_files" \
	  "$$pyc_files" \
	  "$$pyo_files" \
	  "$$pyc_files_pep3147" \
	  "$$pyo_files_pep3147" \
	; do \
	  $(am__uninstall_files_from_dir) || st=$$?; \
	done; \
	exit $$st
install-graph_tool_statsPYTHON: $(graph_tool_stats_PYTHON)
	@$(NORMAL_INSTALL)
	@list='$(graph_tool_stats_PYTHON)'; dlist=; list2=; test -n "$(graph_tool_statsdir)" || list=; \
	if test -n "$$list"; then \
	  echo " $(MKDIR_P) '$(DESTDIR)$(graph_tool_statsdir)'"; \
	  $(MKDIR_P) "$(DESTDIR)$(graph_tool_statsdir)" || exit 1; \
	fi; \
	for p in $$list; do \
	  if test -f "$$p"; then b=; else b="$(srcdir)/"; fi; \
	  if test -f $$b$$p; then \
	    $(am__strip_dir) \
	    dlist="$$dlist $$f"; \
	    list2="$$list2 $$b$$p"; \
	  else :; fi; \
	done; \
	for file in $$list2; do echo $$file; done | $(am__base_list) | \
	while read files; do \
	  echo " $(INSTALL_DATA) $$files '$(DESTDIR)$(graph_tool_statsdir)'"; \
	  $(INSTALL_DATA) $$files "$(DESTDIR)$(graph_tool_statsdir)" || exit $$?; \
	done || exit $$?; \
	if test -n "$$dlist"; then \
	  $(am__py_compile) --destdir "$(DESTDIR)" \
	                    --basedir "$(graph_tool_statsdir)" $$dlist; \
	else :; fi

uninstall-graph_tool_statsPYTHON:
	@$(NORMAL_UNINSTALL)
	@list='$(graph_tool_stats_PYTHON)'; test -n "$(graph_tool_statsdir)" || list=; \
	py_files=`for p in $$list; do echo $$p; done | sed -e 's|^.*/||'`; \
	test -n "$$py_files" || exit 0; \
	dir='$(DESTDIR)$(graph_tool_statsdir)'; \
	pyc_files=`echo "$$py_files" | sed 's|$$|c|'`; \
	pyo_files=`echo "$$py_files" | sed 's|$$|o|'`; \
	py_files_pep3147=`echo "$$py_files" | $(am__pep3147_tweak)`; \
	echo "$$py_files_pep3147";\
	pyc_files_pep3147=`echo "$$py_files_pep3147" | sed 's|$$|c|'`; \
	pyo_files_pep3147=`echo "$$py_files_pep3147" | sed 's|$$|o|'`; \
	st=0; \
	for files in \
	  "$$py_files" \
	  "$$pyc_files" \
	  "$$pyo_files" \
	  "$$pyc_files_pep3147" \
	  "$$pyo_files_pep3147" \
	; do \
	  $(am__uninstall_files_from_dir) || st=$$?; \
	done; \
	exit $$st
install-graph_tool_topologyPYTHON: $(graph_tool_topology_PYTHON)
	@$(NORMAL_INSTALL)
	@list='$(graph_tool_topology_PYTHON)'; dlist=; list2=; test -n "$(graph_tool_topologydir)" || list=; \
	if test -n "$$list"; then \
	  echo " $(MKDIR_P) '$(DESTDIR)$(graph_tool_topologydir)'"; \
	  $(MKDIR_P) "$(DESTDIR)$(graph_tool_topologydir)" || exit 1; \
	fi; \
	for p in $$list; do \
	  if test -f "$$p"; then b=; else b="$(srcdir)/"; fi; \
	  if test -f $$b$$p; then \
	    $(am__strip_dir) \
	    dlist="$$dlist $$f"; \
	    list2="$$list2 $$b$$p"; \
	  else :; fi; \
	done; \
	for file in $$list2; do echo $$file; done | $(am__base_list) | \
	while read files; do \
	  echo " $(INSTALL_DATA) $$files '$(DESTDIR)$(graph_tool_topologydir)'"; \
	  $(INSTALL_DATA) $$files "$(DESTDIR)$(graph_tool_topologydir)" || exit $$?; \
	done || exit $$?; \
	if test -n "$$dlist"; then \
	  $(am__py_compile) --destdir "$(DESTDIR)" \
	                    --basedir "$(graph_tool_topologydir)" $$dlist; \
	else :; fi

uninstall-graph_tool_topologyPYTHON:
	@$(NORMAL_UNINSTALL)
	@list='$(graph_tool_topology_PYTHON)'; test -n "$(graph_tool_topologydir)" || list=; \
	py_files=`for p in $$list; do echo $$p; done | sed -e 's|^.*/||'`; \
	test -n "$$py_files" || exit 0; \
	dir='$(DESTDIR)$(graph_tool_topologydir)'; \
	pyc_files=`echo "$$py_files" | sed 's|$$|c|'`; \
	pyo_files=`echo "$$py_files" | sed 's|$$|o|'`; \
	py_files_pep3147=`echo "$$py_files" | $(am__pep3147_tweak)`; \
	echo "$$py_files_pep3147";\
	pyc_files_pep3147=`echo "$$py_files_pep3147" | sed 's|$$|c|'`; \
	pyo_files_pep3147=`echo "$$py_files_pep3147" | sed 's|$$|o|'`; \
	st=0; \
	for files in \
	  "$$py_files" \
	  "$$pyc_files" \
	  "$$pyo_files" \
	  "$$pyc_files_pep3147" \
	  "$$pyo_files_pep3147" \
	; do \
	  $(am__uninstall_files_from_dir) || st=$$?; \
	done; \
	exit $$st
install-graph_tool_utilPYTHON: $(graph_tool_util_PYTHON)
	@$(NORMAL_INSTALL)
	@list='$(graph_tool_util_PYTHON)'; dlist=; list2=; test -n "$(graph_tool_utildir)" || list=; \
	if test -n "$$list"; then \
	  echo " $(MKDIR_P) '$(DESTDIR)$(graph_tool_utildir)'"; \
	  $(MKDIR_P) "$(DESTDIR)$(graph_tool_utildir)" || exit 1; \
	fi; \
	for p in $$list; do \
	  if test -f "$$p"; then b=; else b="$(srcdir)/"; fi; \
	  if test -f $$b$$p; then \
	    $(am__strip_dir) \
	    dlist="$$dlist $$f"; \
	    list2="$$list2 $$b$$p"; \
	  else :; fi; \
	done; \
	for file in $$list2; do echo $$file; done | $(am__base_list) | \
	while read files; do \
	  echo " $(INSTALL_DATA) $$files '$(DESTDIR)$(graph_tool_utildir)'"; \
	  $(INSTALL_DATA) $$files "$(DESTDIR)$(graph_tool_utildir)" || exit $$?; \
	done || exit $$?; \
	if test -n "$$dlist"; then \
	  $(am__py_compile) --destdir "$(DESTDIR)" \
	                    --basedir "$(graph_tool_utildir)" $$dlist; \
	else :; fi

uninstall-graph_tool_utilPYTHON:
	@$(NORMAL_UNINSTALL)
	@list='$(graph_tool_util_PYTHON)'; test -n "$(graph_tool_utildir)" || list=; \
	py_files=`for p in $$list; do echo $$p; done | sed -e 's|^.*/||'`; \
	test -n "$$py_files" || exit 0; \
	dir='$(DESTDIR)$(graph_tool_utildir)'; \
	pyc_files=`echo "$$py_files" | sed 's|$$|c|'`; \
	pyo_files=`echo "$$py_files" | sed 's|$$|o|'`; \
	py_files_pep3147=`echo "$$py_files" | $(am__pep3147_tweak)`; \
	echo "$$py_files_pep3147";\
	pyc_files_pep3147=`echo "$$py_files_pep3147" | sed 's|$$|c|'`; \
	pyo_files_pep3147=`echo "$$py_files_pep3147" | sed 's|$$|o|'`; \
	st=0; \
	for files in \
	  "$$py_files" \
	  "$$pyc_files" \
	  "$$pyo_files" \
	  "$$pyc_files_pep3147" \
	  "$$pyo_files_pep3147" \
	; do \
	  $(am__uninstall_files_from_dir) || st=$$?; \
	done; \
	exit $$st
install-graph_tool_collectionDATA: $(graph_tool_collection_DATA)
	@$(NORMAL_INSTALL)
	@list='$(graph_tool_collection_DATA)'; test -n "$(graph_tool_collectiondir)" || list=; \
	if test -n "$$list"; then \
	  echo " $(MKDIR_P) '$(DESTDIR)$(graph_tool_collectiondir)'"; \
	  $(MKDIR_P) "$(DESTDIR)$(graph_tool_collectiondir)" || exit 1; \
	fi; \
	for p in $$list; do \
	  if test -f "$$p"; then d=; else d="$(srcdir)/"; fi; \
	  echo "$$d$$p"; \
	done | $(am__base_list) | \
	while read files; do \
	  echo " $(INSTALL_DATA) $$files '$(DESTDIR)$(graph_tool_collectiondir)'"; \
	  $(INSTALL_DATA) $$files "$(DESTDIR)$(graph_tool_collectiondir)" || exit $$?; \
	done

uninstall-graph_tool_collectionDATA:
	@$(NORMAL_UNINSTALL)
	@list='$(graph_tool_collection_DATA)'; test -n "$(graph_tool_collectiondir)" || list=; \
	files=`for p in $$list; do echo $$p; done | sed -e 's|^.*/||'`; \
	dir='$(DESTDIR)$(graph_tool_collectiondir)'; $(am__uninstall_files_from_dir)
install-graph_tool_drawDATA: $(graph_tool_draw_DATA)
	@$(NORMAL_INSTALL)
	@list='$(graph_tool_draw_DATA)'; test -n "$(graph_tool_drawdir)" || list=; \
	if test -n "$$list"; then \
	  echo " $(MKDIR_P) '$(DESTDIR)$(graph_tool_drawdir)'"; \
	  $(MKDIR_P) "$(DESTDIR)$(graph_tool_drawdir)" || exit 1; \
	fi; \
	for p in $$list; do \
	  if test -f "$$p"; then d=; else d="$(srcdir)/"; fi; \
	  echo "$$d$$p"; \
	done | $(am__base_list) | \
	while read files; do \
	  echo " $(INSTALL_DATA) $$files '$(DESTDIR)$(graph_tool_drawdir)'"; \
	  $(INSTALL_DATA) $$files "$(DESTDIR)$(graph_tool_drawdir)" || exit $$?; \
	done

uninstall-graph_tool_drawDATA:
	@$(NORMAL_UNINSTALL)
	@list='$(graph_tool_draw_DATA)'; test -n "$(graph_tool_drawdir)" || list=; \
	files=`for p in $$list; do echo $$p; done | sed -e 's|^.*/||'`; \
	dir='$(DESTDIR)$(graph_tool_drawdir)'; $(am__uninstall_files_from_dir)
tags TAGS:

ctags CTAGS:

cscope cscopelist:


distdir: $(DISTFILES)
	@srcdirstrip=`echo "$(srcdir)" | sed 's/[].[^$$\\*]/\\\\&/g'`; \
	topsrcdirstrip=`echo "$(top_srcdir)" | sed 's/[].[^$$\\*]/\\\\&/g'`; \
	list='$(DISTFILES)'; \
	  dist_files=`for file in $$list; do echo $$file; done | \
	  sed -e "s|^$$srcdirstrip/||;t" \
	      -e "s|^$$topsrcdirstrip/|$(top_builddir)/|;t"`; \
	case $$dist_files in \
	  */*) $(MKDIR_P) `echo "$$dist_files" | \
			   sed '/\//!d;s|^|$(distdir)/|;s,/[^/]*$$,,' | \
			   sort -u` ;; \
	esac; \
	for file in $$dist_files; do \
	  if test -f $$file || test -d $$file; then d=.; else d=$(srcdir); fi; \
	  if test -d $$d/$$file; then \
	    dir=`echo "/$$file" | sed -e 's,/[^/]*$$,,'`; \
	    if test -d "$(distdir)/$$file"; then \
	      find "$(distdir)/$$file" -type d ! -perm -700 -exec chmod u+rwx {} \;; \
	    fi; \
	    if test -d $(srcdir)/$$file && test $$d != $(srcdir); then \
	      cp -fpR $(srcdir)/$$file "$(distdir)$$dir" || exit 1; \
	      find "$(distdir)/$$file" -type d ! -perm -700 -exec chmod u+rwx {} \;; \
	    fi; \
	    cp -fpR $$d/$$file "$(distdir)$$dir" || exit 1; \
	  else \
	    test -f "$(distdir)/$$file" \
	    || cp -p $$d/$$file "$(distdir)/$$file" \
	    || exit 1; \
	  fi; \
	done
check-am: all-am
check: check-am
all-am: Makefile $(DATA)
installdirs:
	for dir in "$(DESTDIR)$(graph_tooldir)" "$(DESTDIR)$(graph_tool_centralitydir)" "$(DESTDIR)$(graph_tool_clusteringdir)" "$(DESTDIR)$(graph_tool_collectiondir)" "$(DESTDIR)$(graph_tool_communitydir)" "$(DESTDIR)$(graph_tool_correlationsdir)" "$(DESTDIR)$(graph_tool_drawdir)" "$(DESTDIR)$(graph_tool_flowdir)" "$(DESTDIR)$(graph_tool_generationdir)" "$(DESTDIR)$(graph_tool_run_actiondir)" "$(DESTDIR)$(graph_tool_searchdir)" "$(DESTDIR)$(graph_tool_spectraldir)" "$(DESTDIR)$(graph_tool_statsdir)" "$(DESTDIR)$(graph_tool_topologydir)" "$(DESTDIR)$(graph_tool_utildir)" "$(DESTDIR)$(graph_tool_collectiondir)" "$(DESTDIR)$(graph_tool_drawdir)"; do \
	  test -z "$$dir" || $(MKDIR_P) "$$dir"; \
	done
install: install-am
install-exec: install-exec-am
install-data: install-data-am
uninstall: uninstall-am

install-am: all-am
	@$(MAKE) $(AM_MAKEFLAGS) install-exec-am install-data-am

installcheck: installcheck-am
install-strip:
	if test -z '$(STRIP)'; then \
	  $(MAKE) $(AM_MAKEFLAGS) INSTALL_PROGRAM="$(INSTALL_STRIP_PROGRAM)" \
	    install_sh_PROGRAM="$(INSTALL_STRIP_PROGRAM)" INSTALL_STRIP_FLAG=-s \
	      install; \
	else \
	  $(MAKE) $(AM_MAKEFLAGS) INSTALL_PROGRAM="$(INSTALL_STRIP_PROGRAM)" \
	    install_sh_PROGRAM="$(INSTALL_STRIP_PROGRAM)" INSTALL_STRIP_FLAG=-s \
	    "INSTALL_PROGRAM_ENV=STRIPPROG='$(STRIP)'" install; \
	fi
mostlyclean-generic:

clean-generic:

distclean-generic:
	-test -z "$(CONFIG_CLEAN_FILES)" || rm -f $(CONFIG_CLEAN_FILES)
	-test . = "$(srcdir)" || test -z "$(CONFIG_CLEAN_VPATH_FILES)" || rm -f $(CONFIG_CLEAN_VPATH_FILES)

maintainer-clean-generic:
	@echo "This command is intended for maintainers to use"
	@echo "it deletes files that may require special tools to rebuild."
clean: clean-am

clean-am: clean-generic clean-libtool mostlyclean-am

distclean: distclean-am
	-rm -f Makefile
distclean-am: clean-am distclean-generic

dvi: dvi-am

dvi-am:

html: html-am

html-am:

info: info-am

info-am:

install-data-am: install-graph_toolPYTHON \
	install-graph_tool_centralityPYTHON \
	install-graph_tool_clusteringPYTHON \
	install-graph_tool_collectionDATA \
	install-graph_tool_collectionPYTHON \
	install-graph_tool_communityPYTHON \
	install-graph_tool_correlationsPYTHON \
	install-graph_tool_drawDATA install-graph_tool_drawPYTHON \
	install-graph_tool_flowPYTHON \
	install-graph_tool_generationPYTHON \
	install-graph_tool_run_actionPYTHON \
	install-graph_tool_searchPYTHON \
	install-graph_tool_spectralPYTHON \
	install-graph_tool_statsPYTHON \
	install-graph_tool_topologyPYTHON \
	install-graph_tool_utilPYTHON

install-dvi: install-dvi-am

install-dvi-am:

install-exec-am:

install-html: install-html-am

install-html-am:

install-info: install-info-am

install-info-am:

install-man:

install-pdf: install-pdf-am

install-pdf-am:

install-ps: install-ps-am

install-ps-am:

installcheck-am:

maintainer-clean: maintainer-clean-am
	-rm -f Makefile
maintainer-clean-am: distclean-am maintainer-clean-generic

mostlyclean: mostlyclean-am

mostlyclean-am: mostlyclean-generic mostlyclean-libtool

pdf: pdf-am

pdf-am:

ps: ps-am

ps-am:

uninstall-am: uninstall-graph_toolPYTHON \
	uninstall-graph_tool_centralityPYTHON \
	uninstall-graph_tool_clusteringPYTHON \
	uninstall-graph_tool_collectionDATA \
	uninstall-graph_tool_collectionPYTHON \
	uninstall-graph_tool_communityPYTHON \
	uninstall-graph_tool_correlationsPYTHON \
	uninstall-graph_tool_drawDATA uninstall-graph_tool_drawPYTHON \
	uninstall-graph_tool_flowPYTHON \
	uninstall-graph_tool_generationPYTHON \
	uninstall-graph_tool_run_actionPYTHON \
	uninstall-graph_tool_searchPYTHON \
	uninstall-graph_tool_spectralPYTHON \
	uninstall-graph_tool_statsPYTHON \
	uninstall-graph_tool_topologyPYTHON \
	uninstall-graph_tool_utilPYTHON

.MAKE: install-am install-strip

.PHONY: all all-am check check-am clean clean-generic clean-libtool \
	cscopelist-am ctags-am distclean distclean-generic \
	distclean-libtool distdir dvi dvi-am html html-am info info-am \
	install install-am install-data install-data-am install-dvi \
	install-dvi-am install-exec install-exec-am \
	install-graph_toolPYTHON install-graph_tool_centralityPYTHON \
	install-graph_tool_clusteringPYTHON \
	install-graph_tool_collectionDATA \
	install-graph_tool_collectionPYTHON \
	install-graph_tool_communityPYTHON \
	install-graph_tool_correlationsPYTHON \
	install-graph_tool_drawDATA install-graph_tool_drawPYTHON \
	install-graph_tool_flowPYTHON \
	install-graph_tool_generationPYTHON \
	install-graph_tool_run_actionPYTHON \
	install-graph_tool_searchPYTHON \
	install-graph_tool_spectralPYTHON \
	install-graph_tool_statsPYTHON \
	install-graph_tool_topologyPYTHON \
	install-graph_tool_utilPYTHON install-html install-html-am \
	install-info install-info-am install-man install-pdf \
	install-pdf-am install-ps install-ps-am install-strip \
	installcheck installcheck-am installdirs maintainer-clean \
	maintainer-clean-generic mostlyclean mostlyclean-generic \
	mostlyclean-libtool pdf pdf-am ps ps-am tags-am uninstall \
	uninstall-am uninstall-graph_toolPYTHON \
	uninstall-graph_tool_centralityPYTHON \
	uninstall-graph_tool_clusteringPYTHON \
	uninstall-graph_tool_collectionDATA \
	uninstall-graph_tool_collectionPYTHON \
	uninstall-graph_tool_communityPYTHON \
	uninstall-graph_tool_correlationsPYTHON \
	uninstall-graph_tool_drawDATA uninstall-graph_tool_drawPYTHON \
	uninstall-graph_tool_flowPYTHON \
	uninstall-graph_tool_generationPYTHON \
	uninstall-graph_tool_run_actionPYTHON \
	uninstall-graph_tool_searchPYTHON \
	uninstall-graph_tool_spectralPYTHON \
	uninstall-graph_tool_statsPYTHON \
	uninstall-graph_tool_topologyPYTHON \
	uninstall-graph_tool_utilPYTHON

.PRECIOUS: Makefile


# Tell versions [3.59,3.63) of GNU make to not export all variables.
# Otherwise a system limit (for SysV at least) may be exceeded.
.NOEXPORT:
